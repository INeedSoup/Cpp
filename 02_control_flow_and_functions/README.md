# 02 — Control flow and functions

Programs become manageable when repetition is explicit and related work is placed in functions. Each function should have one clear job, a meaningful name, and a small interface.

The old `rand` examples have been replaced with `<random>`: it produces a better distribution and makes the random engine explicit. Recursion is useful only when it has a base case and makes the solution clearer than a loop.
